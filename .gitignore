# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.coverage
htmlcov/
dist/
build/

# uv
# (uv.lock IS committed; only the environment is ignored)

# Local harness session state
.pi/

# Eval artifacts and datasets (never committed)
runs/
data/
*.jsonl
